// If else
// Comparison operators
// var, let, const - scope
// Implicit scope - comma separated
// Switch case - with number and string
/* Falsy values: apart from these all are truthy values
    - Boolean false
    - 0 (Number)
    - -0
    - 0n (BigInt)
    - "", '', `` (empty string)
    - null
    - undefined
    - NaN

*/
/* Check for an empty object
  - Object.keys(obj).length === 0 && obj.constructor === Object
*/
/**
 * false == 0 // true
 * false == "" // true
 * false == [] // true
 * 0 == "" // true
 */

// Nullish coalescing operator - ??
/**
 * based on null or undefined
 * a ?? b
 */
